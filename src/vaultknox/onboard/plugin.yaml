name: vaultknox-onboard
description: |
  Autonomous repository onboarding for VaultKnox / Hermes Agent.
  Detects languages, frameworks, and deps; generates AGENTS.md,
  README.md, SETUP.md; installs dependencies and runs build checks.
version: 0.8.3
author: VaultKnox
kind: standalone
provides_hooks:
  - pre_llm_call
