VaultKnox v0.4.1: Fix dormant secret-guard hook — was completely dead code

Fixes:
- Secret-guard hook registered for `message:received` but gateway NEVER emitted that event.
  The hook existed on disk for 5 days doing literally nothing.
- Added `message:received` emitter in gateway/run.py at the message ingress point,
  BEFORE agent:start and BEFORE any persistence.
- Created VaultKnox gateway plugin at ~/.hermes/plugins/vaultknox-secret-guard/
  using pre_gateway_dispatch hook (survives `hermes update`).
- Updated hook handler to support both message:received (content key) and
  agent:start (message key) event types.
- Ran sanitize-history --apply: 2,775 secret occurrences redacted across 35 files
  (session JSONL, state.db, shell history).
